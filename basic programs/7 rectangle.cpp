#include<stdio.h>
#include<conio.h>
main()
{
	int area, l,w;
	printf("enter length : ");
	scanf("%d",&l);
	printf("enter width : ");
	scanf("%d",&w);
	area=l*w;
	printf("circumference of rectangle is %d",area);
}
