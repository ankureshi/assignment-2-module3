#include<stdio.h>
#include<math.h>
main()
{
	int a,b,c,num;
	printf("enter the number..");
	scanf("%d",&num);
	a=pow(num, 1);
	b=pow(num, 2);
	c=pow(num, 3);
	printf("\n power is..\n\n");
	scanf("%d","%d","%d",a,b,c);
	
}
